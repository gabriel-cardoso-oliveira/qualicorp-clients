import { makeStyles } from "@material-ui/styles";

export default makeStyles(theme => ({
  buttonOptions: {
    color: "white",
    textTransform: "none",
    marginLeft: theme.spacing(2),
    height: 28,
    width: 90,
  },
}));
